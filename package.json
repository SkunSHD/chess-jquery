{
  "name": "express-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy-all": "firebase deploy",
    "func-deploy": "firebase deploy --only functions",
    "func-debug": "firebase serve --only functions",
    "kill-3000": "sudo lsof -i :3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
